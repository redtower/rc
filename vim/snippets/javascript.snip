snippet n
    (function() {
        ${1:body}
    })();

snippet c
    console.log(${1:stuff});

snippet f
    function () {}

snippet match
    var m;
    if (m = (videoId.match(/\/.*?[?&;!]v=([^&#?=/;]+)/)||[])[1]) {
    }

snippet for
    for (var i = 0; i < ${1:array}.length; i++) {
        var ${2:single} = ${1:array};
    }

snippet toa
    Array.prototype.slice.apply(${1:body});

